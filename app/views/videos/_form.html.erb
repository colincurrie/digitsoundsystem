<%= form_for @video do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :url %>
  <%= f.text_field :url %>

  <%= f.submit %>

  <%= render 'shared/errors', object: @video %>

<% end %>
