<% if current_user %>
  <%= link_to 'Add Comment', '#new_comment', class: 'new_comment' %>
<% end %>

<%= form_for([@story, @story.comments.build]) do |f| %>
  <%= f.text_area :content, placeholder: 'Comment' %>
  <%= f.submit %>
<% end %>
