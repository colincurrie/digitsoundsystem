<%= form_for @tune do |f| %>

  <%= f.label :url %><br>
  <%= f.text_field :url %>

  <%= f.label :artist %><br>
  <%= f.text_field :artist %>

  <%= f.label :title %><br>
  <%= f.text_field :title %>

  <%= f.submit %>

  <%= render 'shared/errors', object: @tune %>

<% end %>
