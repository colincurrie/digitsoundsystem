<div class="actions">
  <%= link_to 'Edit', edit_story_path(@story) %>
  <%= link_to 'Back to News', stories_path %>
</div>
<h1><%= @story.title %></h1>

<p><%= @story.content %></p>

<h2>Comments:</h2>
<div class="comments"><%= render @comments, parent: @story %></div>
<%= render 'comments/form', parent: @story %>
<%= will_paginate @comments %>
