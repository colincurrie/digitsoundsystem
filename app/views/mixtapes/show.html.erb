<h1><%= link_to @mixtape.title, @mixtape.url %></h1>
<p><%= @mixtape.description %></p>

<% if current_user.try :admin? %>
  <div class="actions">
    <%= link_to 'Edit', edit_photo_path(@mixtape) %>
    <%= link_to 'Back', mixtapes_path %>
  </div>
<% end %>
